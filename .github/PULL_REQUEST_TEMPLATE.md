## What

One-paragraph summary of the change.

## Why

Motivation — link the issue if there is one.

## Checklist

- [ ] `npm test` passes (schemas compile, enums in sync, examples validate)
- [ ] Schema changes are mirrored in `tools/eap.ts` const maps
- [ ] Spec text changes keep SPEC.md, CONFORMANCE.md, and schemas consistent
- [ ] CHANGELOG.md updated under `Unreleased` for normative changes
